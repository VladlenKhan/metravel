🏖 Metravel — Веб-приложение турфирмы

Metravel — это современное веб-приложение для турфирмы, разработанное с использованием React, TypeScript, Vite и Tailwind CSS на фронтенде и ASP.NET Core Web API на бэкенде.
Приложение позволяет пользователям просматривать туры, бронировать их, а администраторам — управлять турами и бронированиями.

🔹 Стек технологий

Frontend:

React 18 + TypeScript

Vite (сборка и разработка)

Tailwind CSS (стилизация)

React Router (маршрутизация)

Axios (HTTP-запросы к backend)

Backend:

ASP.NET Core Web API (C#)

Entity Framework Core

JWT аутентификация

Swagger / OpenAPI

База данных: PostgreSQL / MS SQL (в зависимости от настроек backend)

🔹 Функционал
Пользователь:

Регистрация и авторизация

Просмотр всех доступных туров

Просмотр деталей тура

Бронирование тура

Просмотр своих бронирований

Администратор:

CRUD операций с турами (создание, редактирование, удаление)

Просмотр всех бронирований пользователей

🔹 Структура проекта
metravel/
 ├── backend/       # ASP.NET Core Web API
 └── frontend/      # React + Vite + TypeScript + Tailwind
      ├── src/
      │    ├── app/           # Настройки роутера и axios
      │    ├── pages/         # Страницы приложения
      │    ├── components/    # UI-компоненты
      │    ├── features/      # Логика авторизации, бронирований и туров
      │    └── types/         # Типы TypeScript
      ├── index.html
      └── package.json

🔹 Установка и запуск
1. Backend

Перейти в папку backend:

cd metravel/backend


Восстановить зависимости:

dotnet restore


Применить миграции (если используется EF Core):

dotnet ef database update


Запустить сервер:

dotnet run


Backend обычно доступен: https://localhost:5001

2. Frontend

Перейти в папку frontend:

cd metravel/frontend


Установить зависимости:

npm install
npm install -D tailwindcss postcss autoprefixer
npm install axios react-router-dom


Настроить Tailwind (если нужно, вручную создать tailwind.config.js и postcss.config.js)

Запустить фронтенд:

npm run dev


Приложение доступно: http://localhost:5173

🔹 Настройка CORS (обязательно для работы frontend с backend)

В backend добавить:

builder.Services.AddCors(options =>
{
    options.AddPolicy("AllowFrontend",
        policy => policy
            .WithOrigins("http://localhost:5173")
            .AllowAnyHeader()
            .AllowAnyMethod());
});

app.UseCors("AllowFrontend");

🔹 Использование

Пользователи могут просматривать туры и бронировать их

Администраторы могут управлять турами и просматривать бронирования

JWT-токены используются для авторизации и разграничения ролей

🔹 Цель проекта

Проект выполнен в рамках курсовой работы и демонстрирует:

SPA с React и TypeScript

Интеграцию с REST API (C# / ASP.NET Core)

CRUD операции

Аутентификацию и авторизацию через JWT

Адаптивный и современный дизайн с Tailwind CSS